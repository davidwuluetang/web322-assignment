<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Lego Collection</title>
        <link rel="stylesheet" href="/css/main.css"/>
    </head>
    <body tabIndex="-1">
      <%- include('partials/navbar', {page: '/lego/sets'}) %>

        <div class="container mx-auto">
            <div class="hero bg-base-300 p-6">
                <div class="hero-content text-center">
                  <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Collection</h1>
                    <p class="py-6">Browse the entire collection.  Click the theme to filter by themes such as: 
                        <a href="/lego/sets?theme=Technic" class="btn btn-primary btn-xs">Technic</a> 
                        <a href="/lego/sets?theme=Basic Set" class="btn btn-primary btn-xs">Basic Set</a> 
                        <a href="/lego/sets?theme=Supplemental" class="btn btn-primary btn-xs">Supplemental</a> 
                        <a href="/lego/sets?theme=Jumbo Bricks" class="btn btn-primary btn-xs">Jumbo Bricks</a> 
                        <a href="/lego/sets?theme=Town" class="btn btn-primary btn-xs">Town</a>
                    </p>
                  </div>
                </div>
            </div>

            <div class="overflow-x-auto">
                <table class="table">
                    <% sets.forEach(set=>{ %>
                        <tr>
                          <td>
                            <div class="avatar">
                                <div class="w-32 rounded">
                                <img src="<%= set.img_url %>" alt="preview"  />
                                </div>
                            </div>
                          </td>
                          <td><%= set.name %> --- <a href="/lego/sets?theme=<%= set.theme %>" class="btn btn-primary btn-xs"><%= set.theme %></a></td>
                          <td><strong>Year: </strong><%= set.year %> - <strong>Part: </strong><%= set.num_parts %></td>
                          <td><a href="/lego/sets/<%= set.set_num %>" class="btn btn-secondary btn-sm">Details</a></td>
                        </tr>
                    <% }) %>
                </table>
              </div>

        </div>

    </body>
</html>